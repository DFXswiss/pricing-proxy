# CoinGecko Pro API key (https://www.coingecko.com/en/api/pricing).
# The proxy injects this as the `x-cg-pro-api-key` header on every upstream
# request, so the key lives here alone and never ships in consumer envs.
COINGECKO_API_KEY=
